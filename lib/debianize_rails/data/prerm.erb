#!/bin/sh
set -e

<% if create_passenger_config %>
<%= "rm /etc/apache2/sites-enabled/#{package_name}" %>
<%= "rm /etc/apache2/sites-available/#{package_name}" %>
/etc/init.d/apache2 restart
<% end %>
